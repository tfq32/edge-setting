# Edge Setting 微应用 — 产品需求文档 (PRD)

> 版本: v1.4.0-draft  
> 日期: 2025-07-24  
> 作者: 小螃蟹 🦀

---

## 1. 产品概述

### 1.1 产品名称
Edge Setting 微应用

### 1.2 产品定位
运行在 Linux 边缘节点上的**设置类微应用**，作为众多微应用之一，由底层系统管理服务统一管理。为运维人员提供系统状态监控、微应用运行状态查看与管控、系统版本信息展示等能力。

### 1.3 核心价值
- 在 Pad / 手机端即可完成边缘节点的日常运维巡检
- 无需 SSH 登录服务器，通过 Web UI 查看和管理所有微应用
- 通过 VSOA 与系统管理服务通信，实时获取微应用运行状态

### 1.4 目标用户
- 系统运维人员
- 项目管理员
- 现场实施工程师

---

## 2. 技术架构

### 2.1 系统位置

```
Linux 边缘节点
┌─────────────────────────────────────────────────────┐
│                                                      │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────┐ │
│   │ 微应用 A │ │ 微应用 B │ │ Setting  │ │  ...  │ │
│   │          │ │          │ │ (本项目)  │ │       │ │
│   └────┬─────┘ └────┬─────┘ └────┬─────┘ └──┬────┘ │
│        │             │            │           │      │
│   ─────┴─────────────┴────────────┴───────────┴───── │
│                    VSOA 软总线                        │
│   ───────────────────────┬─────────────────────────── │
│                          │                            │
│                ┌─────────┴─────────┐                  │
│                │  系统管理服务 (MS) │                  │
│                │  微应用生命周期管理 │                  │
│                │  系统资源监控      │                  │
│                └───────────────────┘                  │
│                                                      │
│                    Linux OS                           │
└─────────────────────────────────────────────────────┘
```

**Setting 微应用**是众多微应用中的一个，与其他微应用处于同一层级，统一由系统管理服务管理。Setting 的 Go 后端通过 VSOA 与系统管理服务通信，获取所有微应用的运行状态并提供管控能力。

### 2.2 技术栈明细

#### 后端技术栈

| 类别 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| **语言** | Go | 1.25 | 编译为单一静态二进制 |
| **HTTP 框架** | Gin | 1.12.0 | 高性能 HTTP 框架，支持中间件 |
| **WebSocket** | gorilla/websocket | 1.5.3 | 实时指标推送、日志流 |
| **微服务通信** | VSOA Go SDK | — | 与系统管理服务通信 |
| **数据存储** | modernc.org/sqlite | 1.35.0 | 纯 Go 实现，无 CGO 依赖，支持静态编译 |
| **配置管理** | Viper | 1.20.1 | YAML 配置文件解析 |
| **日志框架** | Zap | 1.27.0 | Uber 高性能结构化日志 |
| **系统指标采集** | gopsutil | 4.25.7 | CPU/内存/磁盘/网络采集 |
| **构建工具** | Make + GoReleaser | — | 交叉编译 amd64/arm64 |

#### 前端技术栈

| 类别 | 技术选型 | 版本 | 说明 |
|------|---------|------|------|
| **框架** | Vue 3 | 3.5.30 | Composition API + `<script setup>` |
| **构建工具** | Vite | 8.0.0 | 快速构建 + HMR |
| **语言** | TypeScript | 5.9.3 | 类型安全 |
| **状态管理** | Pinia | 3.0.4 | Vue 3 官方推荐 |
| **路由** | Vue Router | 5.0.3 | 单页应用路由 |
| **UI 组件库** | Vant 4 | 4.9.14 | 移动端 UI 组件，轻量适配手机/Pad |
| **图表库** | ECharts | 6.0.0 | 折线图/环形图/面积图 |
| **HTTP 客户端** | Axios | 1.13.6 | API 请求封装 |
| **CSS 方案** | Tailwind CSS | 4.2.1 | 原子化 CSS，快速响应式布局 |
| **适配方案** | postcss-px-to-viewport | 1.1.1 | px → vw/vh 自动转换 |
| **包管理** | pnpm | 10.32.1 | 快速、节省磁盘 |

#### VSOA 通信（与系统管理服务交互）

| 能力 | Go SDK API | 说明 |
|------|-----------|------|
| **查询微应用列表** | `client.Call(ctx, GET, url, ...)` | 向系统管理服务 RPC 查询所有微应用状态 |
| **订阅状态变更** | `client.Subscribe(url, handler)` | 订阅微应用上下线 / 状态变更事件 |
| **管控微应用** | `client.Call(ctx, SET, url, ...)` | 通过系统管理服务启动/停止/重启指定微应用 |
| **获取系统信息** | `client.Call(ctx, GET, url, ...)` | 查询系统版本、架构等信息 |

#### 部署技术栈

| 类别 | 技术选型 | 说明 |
|------|---------|------|
| **部署形态** | 单一二进制 + 静态前端 | 前端可内嵌（`embed.FS`）或独立目录 |
| **运行管理** | 由系统管理服务统一管理 | Setting 作为微应用被管理 |
| **目标平台** | Linux amd64 / arm64 | 交叉编译支持 |
| **配置格式** | YAML | `config.yaml` |

### 2.3 技术约束

| 项目 | 约束 |
|------|------|
| 主题色 | `#42AAF5` |
| UI 风格 | 简约 / 飞书移动端风格 |
| 适配终端 | 手机端（≥375px）+ Pad 端（≥768px）响应式 |
| 前后端隔离 | 独立模块，通过 REST API + WebSocket 通信 |
| 微应用通信 | 仅通过 VSOA 与系统管理服务交互 |
| 部署模式 | 支持分布式部署 |

---

## 3. 功能模块

### 3.1 系统状态监控

#### 3.1.1 实时指标看板

| 指标 | 说明 | 采集方式 | 刷新频率 |
|------|------|---------|----------|
| CPU 占用率 | 总体 + 各核心百分比 | gopsutil `cpu.Percent()` | ≤ 3s |
| 内存使用 | 已用 / 总量 / 可用，含 swap | gopsutil `mem.VirtualMemory()` | ≤ 3s |
| 磁盘 I/O | 各挂载点读写速率（MB/s）、IOPS | gopsutil `disk.IOCounters()` | ≤ 5s |
| 网络吞吐 | 各网卡上行/下行速率（Mbps） | gopsutil `net.IOCounters()` | ≤ 3s |
| 系统负载 | 1min / 5min / 15min load average | gopsutil `load.Avg()` | ≤ 5s |
| 运行时间 | uptime | gopsutil `host.Uptime()` | ≤ 60s |

**交互要求：**
- 顶部以卡片形式展示当前核心指标概览（CPU / 内存 / 磁盘 / 网络）
- 每个指标卡片使用 ECharts 环形图或进度条，超过阈值（如 CPU > 80%）变色告警
- 点击卡片进入该指标的详情页

#### 3.1.2 历史趋势图表

| 项目 | 说明 |
|------|------|
| 时间范围 | 最近 1 小时 / 6 小时 / 24 小时 / 7 天 |
| 图表类型 | ECharts 折线图（面积填充） |
| 数据粒度 | 1h 内 → 3s 原始点；6h → 30s 聚合；24h → 5min 聚合；7d → 1h 聚合 |
| 存储 | SQLite，保留 7 天数据，自动清理 |

**交互要求：**
- 支持手势缩放和滑动查看（ECharts dataZoom）
- 长按某时间点显示精确数值 tooltip
- 支持多指标叠加对比

---

### 3.2 微应用运行状态

#### 3.2.1 数据获取机制

Setting 后端通过 VSOA 与系统管理服务通信：

```
Setting (Go 后端)                   系统管理服务 (MS)
      │                                     │
      │── VSOA RPC GET (查询微应用列表) ────▶│
      │◀── [{name, status, pid, cpu, ...}] ─│
      │                                     │
      │── VSOA Subscribe (状态变更) ───────▶│
      │◀── 事件推送: 微应用上线/下线/异常 ──│
      │                                     │
      │── VSOA RPC SET (启动微应用X) ──────▶│
      │◀── {result: "ok"} ─────────────────│
```

- 通过 **RPC GET** 主动查询当前所有微应用的运行状态
- 通过 **Subscribe** 订阅微应用状态变更事件，实时更新
- 通过 **RPC SET** 向系统管理服务发送控制指令（启动/停止/重启）

#### 3.2.2 微应用列表

以**卡片列表**形式展示所有微应用（Vant Card 组件），每张卡片包含：

| 字段 | 说明 |
|------|------|
| 微应用名称 | 显示名 + ID |
| 运行状态 | 运行中（绿）/ 已停止（灰） |
| 应用分类 | 系统应用 / 用户应用 |
| 应用类型 | 有页面（前台应用）/ 后台服务 |
| 启动时间 | 上次启动的时间戳 |
| CPU 占用 | 该微应用当前 CPU 使用百分比 |
| 内存占用 | 该微应用当前 RSS 内存 |
| 端口 | 监听端口列表 |

**交互要求：**
- 列表支持按状态筛选（全部 / 运行中 / 已停止 / 异常）
- 支持搜索微应用名称
- 卡片右侧显示操作按钮或点击进入详情

#### 3.2.3 微应用详情页

| 功能 | 说明 |
|------|------|
| 基本信息 | 微应用名、版本号、启动参数、PID、监听端口 |
| 资源趋势 | 该微应用近 1h 的 CPU / 内存趋势小图（ECharts） |
| 运行日志 | 实时滚动展示最新日志，支持关键字过滤 |
| 历史日志 | 支持按时间范围查询历史日志 |
| 操作按钮 | 启动 / 停止 / 重启（带二次确认弹窗） |

#### 3.2.4 微应用管控操作

所有管控操作通过 VSOA 发送给系统管理服务执行：

| 操作 | 说明 |
|------|------|
| 启动 | 通知系统管理服务启动指定微应用 |
| 停止 | 通知系统管理服务停止指定微应用 |
| 重启 | 通知系统管理服务重启指定微应用 |

**安全约束：**
- 所有操作需记录操作日志（谁、什么时间、对哪个微应用做了什么）
- 停止/重启操作需二次确认

#### 3.2.5 日志持久化

- 各微应用日志持久化存储到本地磁盘（SQLite 结构化存储）
- 支持按微应用、时间范围、关键字查询历史日志
- 日志保留策略：按大小或天数自动轮转清理（可配置）
- 存储格式：结构化日志（时间戳 + 级别 + 微应用名 + 内容）

---

### 3.3 系统信息

| 字段 | 说明 | 采集方式 |
|------|------|---------|
| 操作系统 | 发行版名称 + 版本 | gopsutil `host.Info()` |
| 内核版本 | Linux kernel version | gopsutil `host.Info()` |
| 架构 | amd64 / arm64 | `runtime.GOARCH` |
| 主机名 | hostname | `os.Hostname()` |
| 系统架构版本 | 当前部署的整体平台版本号 | VSOA 查询或配置文件 |
| 前端版本 | 前端构建版本号 | 编译时注入 |
| 后端版本 | 后端构建版本号 | 编译时注入（`ldflags`） |

---

## 4. 非功能需求

### 4.1 性能

| 指标 | 要求 |
|------|------|
| 后端内存占用 | ≤ 50MB（空闲态） |
| CPU 占用 | ≤ 2%（采集态） |
| API 响应时间 | P95 ≤ 200ms |
| WebSocket 推送延迟 | ≤ 500ms |
| 并发连接 | 支持 ≥ 20 个同时在线客户端 |

### 4.2 兼容性

| 终端 | 最低分辨率 |
|------|-----------|
| 手机 | 375 × 667（iPhone SE） |
| Pad | 768 × 1024（iPad Mini） |
| 浏览器 | Chrome 90+ / Safari 15+ / Firefox 90+ |

### 4.3 安全

- API 通信使用 HTTPS（可选，支持自签证书）
- 认证方式：待确认（见开放问题）
- 微应用管控操作记录审计日志

### 4.4 部署

- 后端编译为**单一二进制文件**（纯 Go 静态编译，SQLite 使用 modernc 版本）
- 前端构建产物内嵌（`embed.FS`）或独立静态目录
- 作为微应用由系统管理服务统一部署和管理
- 提供 `config.yaml` 配置文件（端口、数据目录、日志级别、日志保留策略等）
- 支持 arm64 交叉编译（适配边缘硬件）

---

## 5. API 设计概要

### 5.1 通信方式

| 类型 | 技术 | 用途 |
|------|------|------|
| REST API | Gin/Chi + JSON | 前端查询和操作（系统信息、微应用管理、日志查询） |
| WebSocket | gorilla/websocket | 实时指标推送、实时日志流 |
| VSOA | Go SDK | 后端与系统管理服务通信（获取微应用状态、下发管控指令） |

### 5.2 前端 → Setting 后端接口

```
GET    /api/v1/system/info            # 系统信息
GET    /api/v1/system/metrics         # 当前实时指标
WS     /api/v1/system/metrics/ws      # 实时指标推送（WebSocket）

GET    /api/v1/metrics/history        # 历史指标查询 ?type=cpu&range=1h

GET    /api/v1/apps                   # 微应用列表（后端从 MS 获取）
GET    /api/v1/apps/:id               # 微应用详情
POST   /api/v1/apps/:id/start         # 启动微应用（后端转发 MS）
POST   /api/v1/apps/:id/stop          # 停止微应用
POST   /api/v1/apps/:id/restart       # 重启微应用
WS     /api/v1/apps/:id/logs          # 实时日志流（WebSocket）
GET    /api/v1/apps/:id/logs          # 历史日志查询 ?from=&to=&keyword=

GET    /api/v1/version                # 系统架构版本
```

---

## 6. 前端页面结构

```
├── 首页（Dashboard）
│   ├── 系统指标概览卡片（CPU / 内存 / 磁盘 / 网络）— ECharts 环形图
│   ├── 微应用状态摘要（运行中 x 个 / 异常 x 个）— Vant Badge
│   └── 系统版本信息
│
├── 系统监控
│   ├── 实时指标详情 — ECharts Gauge
│   └── 历史趋势图表 — ECharts Line + dataZoom
│
├── 微应用管理
│   ├── 微应用列表（Vant Card + 筛选 + 搜索）
│   └── 微应用详情（信息 + 日志 + 历史日志 + 操作）
│
└── 关于
    └── 系统信息 + 版本号 — Vant Cell
```

**导航方式：**
- 手机端 → Vant Tabbar（底部 Tab 栏）
- Pad 端 → 侧边栏导航（CSS 媒体查询自适应切换）

---

## 7. 项目结构

```
edge-setting/
├── backend/                  # Go 后端（Setting 微应用）
│   ├── cmd/server/           # 入口 main.go
│   ├── internal/
│   │   ├── api/              # HTTP handler（Gin/Chi 路由）
│   │   ├── ws/               # WebSocket 管理（gorilla/websocket）
│   │   ├── collector/        # 本机系统指标采集（gopsutil）
│   │   ├── vsoa/             # VSOA 客户端封装（与系统管理服务通信）
│   │   ├── app/              # 微应用管理业务逻辑
│   │   ├── store/            # SQLite 存储（指标历史 + 日志持久化）
│   │   ├── logger/           # 结构化日志（Zap/Zerolog）
│   │   └── config/           # Viper 配置管理
│   ├── go.mod
│   ├── Makefile
│   └── .goreleaser.yml
│
├── frontend/                 # Vue 3 前端
│   ├── src/
│   │   ├── views/            # 页面组件
│   │   ├── components/       # 通用组件（图表、卡片等）
│   │   ├── composables/      # 组合式函数（useMetrics, useWebSocket 等）
│   │   ├── api/              # Axios API 封装
│   │   ├── stores/           # Pinia 状态管理
│   │   ├── router/           # Vue Router 路由配置
│   │   └── styles/           # 全局样式 + 主题变量（#42AAF5）
│   ├── package.json
│   ├── pnpm-lock.yaml
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── uno.config.ts         # UnoCSS 配置
│
├── configs/                  # 部署配置示例
│   └── config.yaml
│
├── scripts/                  # 构建/部署脚本
│   ├── build.sh
│   └── deploy.sh
│
└── .product/                 # 产品文档
    └── doc/
        └── 产品需求.md
```

---

## 8. 开放问题

| # | 问题 | 状态 |
|---|------|------|
| 1 | 认证方式：VSOA JWT / 账号密码 / 暂不做？ | **待确认** |

---

*文档结束。有问题随时提，确认后可以开始搭骨架。*
